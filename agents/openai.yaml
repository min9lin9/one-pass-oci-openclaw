interface:
  display_name: "One-pass OCI OpenClaw"
  short_description: "Provision and operate private OpenClaw + Buzz on Oracle ARM"
  default_prompt: "Use $one-pass-oci-openclaw to build my Oracle OpenClaw + Buzz environment from OCI variables, with isolated operations/planning/development profiles, GBrain/gstack on operations, OpenCode API or ChatGPT auth, and the curated extension bundle."
policy:
  allow_implicit_invocation: false
